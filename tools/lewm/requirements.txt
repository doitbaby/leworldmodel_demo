torch>=2.1
einops>=0.7
numpy>=1.24

# Inference sidecar (M3+). FastAPI's TestClient also requires httpx.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27

# Plotting for M9 ablation / latent visualization / surprise-score
# analysis scripts under tools/lewm/scripts/. Only used by offline
# tooling; the train loop and sidecar do not import matplotlib.
matplotlib>=3.7
