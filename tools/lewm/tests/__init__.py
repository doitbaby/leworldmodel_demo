"""Lightweight checks for the LeWM port.

These are not full unit tests (no pytest dependency yet); they are
runnable Python modules that assert basic invariants and exit non-zero
on failure. They are designed to be cheap enough for the M7 CI workflow.
"""
