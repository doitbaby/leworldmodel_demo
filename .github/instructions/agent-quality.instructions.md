---
applyTo: "**"
---

# Agent Quality Instructions

Use `AGENTS.md` as the source of truth. For non-trivial tasks, follow this
sequence:

```text
research -> plan -> implement -> verify -> review -> docs -> final summary
```

Do not make broad refactors without an explicit request. Preserve user changes.
Prefer the smallest meaningful verification command and state exactly what was
run.
